import Layout from '../components/Layout'

export default function Track({ session }) {
  return (
    <Layout session={session}>
      <p className="text-white">Track page coming soon</p>
    </Layout>
  )
}